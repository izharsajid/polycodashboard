# Local only. Netlify injects these inside a function, so they are needed just for
# scripts run from a laptop, such as the one that sets the first administrator's
# password. Never commit a real value.
NETLIFY_SITE_ID=
NETLIFY_BLOBS_TOKEN=
